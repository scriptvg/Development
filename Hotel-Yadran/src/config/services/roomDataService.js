const formatRoomData = (room) => ({    id: room.id,    nombre: room.name || room.nombre,    tipo: room.type || room.tipo,    precio: room.price || room.precio,    capacidad: room.capacity || room.capacidad,    descripcion: room.description || room.descripcion,    estado: room.status || room.estado,    servicios: room.services || room.servicios || [],    imagen: room.image || room.imagen});const getRoomDetails = (roomData) => ({    ...formatRoomData(roomData),    disponible: roomData.available || roomData.disponible || false});export const roomDataService = {    formatRoomData,    getRoomDetails};export default roomDataService;